using System;

namespace com.CIMthetics.CSharpSECSTools.SECSCommUtils
{
	public enum HSMSConnectionMode
	{
		Passive,
		Active,
		PassivePassthru, // Don't handle the HSMS control messages
		ActivePassthru  // Don't handle the HSMS control messages
	}
}

